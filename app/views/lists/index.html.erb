<div class="list-wrap">
  <% @books.each do |book| %>
</div>


<div class="list-comment">
  <span class="book-body"><%= book.body %></span>
  <span class="book-title"><%= book.title %></span>
</div>
<% end %>

